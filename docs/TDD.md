# Technical Design Document

The TDD lives as a shared document (30 sections) so you can both comment and edit inline:

**https://claude.ai/code/artifact/9d9e2352-fd7c-4c3b-a396-77b5b3a8e636**

(Share the doc with your teammate from its share menu if they cannot open the link.)

To keep a copy in the repo, use the document's export to Markdown and replace this file with it.
Sections most used while building: 10 (modules), 13 (database), 14 (API), 15 (AI), 19 (security), 26 (task list).
