Next.js allows one dynamic segment name per level, so `[param]` is the provider ("google" | "slack")
for `start` and `callback`, and the connection id for DELETE. The TDD names them {provider} and {id}.
