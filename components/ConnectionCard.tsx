/** TODO(T16): Provider card: status, connected date, Google 7-day note, real-account warning, Connect/Reconnect/Disconnect. */
export function ConnectionCard(_props: { provider: string }) {
  return null;
}
