/** TODO(T19): Confirm dialog: repeats the ApprovalSummary and says 'No change will be made unless you confirm.' Buttons: Confirm, Choose a different option, Reject, Exit. */
export function ApprovalDialog(_props: { open: boolean }) {
  return null;
}
